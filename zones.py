from telegram import Update
from telegram.ext import ContextTypes

async def zones_now(update: Update, context: ContextTypes.DEFAULT_TYPE):
    raise NotImplementedError("zones_now belum dipindahkan")
